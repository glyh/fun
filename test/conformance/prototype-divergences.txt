# Cases the OCaml prototype is known to fail. The shared .expect states the
# language's behaviour (the domain model is the specification); these are
# prototype defects, fixed in the .NET port only. Each line names the ticket.
#
# The OCaml runner expects every case listed here to FAIL, and reports one that
# passes so it can be removed. The .NET runner ignores this file.
#
# <area>/<name>    <ticket in docs/wayfinder/tickets/>
elaborate/elab-049                          lambda-check-ignores-written-parameter-type.md
elaborate/lambda-param-type-mismatch        lambda-check-ignores-written-parameter-type.md
elaborate/meta-solution-dependent-spine     meta-solution-renaming-not-lifted-under-binders.md

// The `fun` CLI. Nothing to drive yet -- the pipeline is being ported one
// vertical slice at a time; see docs/wayfinder/tickets/port-core-tt-to-dotnet.md.
Console.Error.WriteLine("fun: the .NET port has no entry point yet");
return 1;

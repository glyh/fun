<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\Fun.Kernel\Fun.Kernel.csproj" />
    <ProjectReference Include="..\Fun.Expand\Fun.Expand.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- The prelude's source, the port's own copy (docs/wayfinder/tickets/port-core-tt-to-dotnet.md). -->
    <EmbeddedResource Include="..\..\std\*.fun" LogicalName="std/%(Filename)%(Extension)" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>

# Wiring Checklist: Features needed before connecting to old pipeline

## Blocking

- [x] `let rec` / `Fix` in surface syntax — no way to express recursion currently
- [ ] Literal patterns — matching on `42`, `true`, `'a'`, `()`
- [ ] Tuple patterns — destructuring `(a, b, c)` in match branches
- [ ] Or-patterns — `A | B -> ...` combining alternatives
- [ ] Record patterns — `Foo {x; y}` or `Foo {x; _}` destructuring

## Probably blocking

- [ ] Polymorphic `==`/`!=` — currently `I64 -> I64 -> Bool`, old pipeline has `{a} -> a -> a -> Bool`

## Lower priority

- [ ] Decision-tree match compilation (linear scan is correct, just no exhaustiveness)
- [ ] Record-type-directed field inference (partial-struct unification covers some cases)
